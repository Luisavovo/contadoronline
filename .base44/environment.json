{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "Signing secret for JWT auth tokens. A development placeholder was generated; replace with your own for production.",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    }
  ]
}
